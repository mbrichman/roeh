# INDEX — decision-trace.md

*Generated by `roeh index`. Do not edit; regenerate.*

**Read this in full before answering, at any trace size.** It is the map
of what the record contains. Then `roeh read <§N|YYYY-MM-DD>` to pull the
chapters you actually need. Grepping the trace directly without reading
this first is how you confidently miss the entry that overturns your answer.

Trace: 453 lines · 4 chapters · 42 tagged entries

## Sections

- **§0** — lines 12–28 · `roeh read §0`
- **§1** — lines 29–68 · `roeh read §1`
- **§2** — lines 69–84 · `roeh read §2`
- **§3** — lines 85–220 · `roeh read §3`
- **§4** — lines 221–244 · `roeh read §4`
- **§5** — lines 245–336 · `roeh read §5`
- **§5** — lines 337–453 · `roeh read §5`

## Chapters

- **Contradictions & staleness ledger** — lines 75–84 · 0 entries · `roeh read "Contradictions & staleness ledger"`
- **2026-08-16 — roeh built, from a working single-project system** — lines 87–220 · 23 entries · `roeh read "2026-08-16"`
- **2026-08-17 — first refresh** — lines 287–336 · 7 entries · `roeh read "2026-08-17"`
- **2026-08-09 — origin (recorded 2026-08-17, out of chronological order)** — lines 362–453 · 4 entries · `roeh read "2026-08-09"`

## Supersessions and dead-ends — check these before quoting anything

- `REVERSAL` L99 (2026-08-16) — The scribe may write, through an append-only CLI.** The inherited design forbade it outright · `b8de529`
- `REVERSAL` L159 (2026-08-16) — `--force` merges; it no longer resets.** It had overwritten the config wholesale, so a `local`-mode trace — lo
- `CORRECTION` L280 (2026-08-16) — They are not, reliably; reviewers skim them
- `CORRECTION` L305 (2026-08-17) — They were appended before the work was committed, so they cite skill-file sections rather than a SHA
- `CORRECTION` L311 (2026-08-17) — It records v0.3.4
- `CORRECTION` L408 (2026-08-09) — ` immediately above]** That entry claimed the origin sentence "survived only because the owner kept it and pas

## All entries

- `PRINCIPLE` L30 (§1) — append-only is structural, not honour-system.** The scribe has no `Write` or `Edit` tool, and `roeh append` op
- `PRINCIPLE` L38 (§1) — two records is worse than none.** Never orphan, duplicate or silently repoint a trace
- `PRINCIPLE` L43 (§1) — under-claim beats confabulate.** "The record does not cover this" is a correct and valuable answer
- `PRINCIPLE` L47 (§1) — assistant prose is never a source.** Learnings come from the owner's turns, the code, PR artifacts and measure
- `PRINCIPLE` L51 (§1) — judgement is pinned; extraction is cheap.** WHY: the two are different jobs and fail differently
- `PRINCIPLE` L54 (§1) — a guard that is never exercised is not a guard.** WHY: four bugs shipped here with exactly that shape before a
- `PRINCIPLE` L58 (§1) — prefer loud failure to soft failure.** A gate whose result must be inferred, a check that silently degrades, a
- `PRINCIPLE` L62 (§1) — never infer personal identity from a filesystem path.** WHY: a directory name is not a legal name, and a publi
- `DECISION` L90 (2026-08-16) — Split the Oracle into invariant method plus a generated profile.** `agents/oracle.md` holds only the method —
- `REVERSAL` L99 (2026-08-16) — The scribe may write, through an append-only CLI.** The inherited design forbade it outright · `b8de529`
- `DECISION` L105 (2026-08-16) — Hook policy differs by trigger.** Manual `/compact`: dispatch the scribe, block with `exit 2` if still behind, · `b8de529`
- `DECISION` L111 (2026-08-16) — Make the hook handshake order-independent.** Handlers within one event have no documented execution order, so · `b8de529`
- `DECISION` L117 (2026-08-16) — `SessionStart` on compact/clear/fork re-injects the pointer and §5.** WHY: writing the record is pointless if · `b8de529`
- `DECISION` L120 (2026-08-16) — Author metadata uses the GitHub handle, not a personal name.** WHY: the name had been inferred from a local di · `e1b08ea`, reaffirmed `a52dd6f`
- `DECISION` L124 (2026-08-16) — Pin judgement to `opus`; run extraction on `sonnet`.** Nothing declared a model, so everything inherited the s
- `DECISION` L135 (2026-08-16) — No `roeh update` command.** The harness already has `claude plugin update`, `marketplace update`, `tag` and `/ · `20914b6`
- `GOTCHA` L141 (2026-08-16) — `SCHEMA_VERSION` was stamped into every config and never read back.** `load_config` merged the file over the d · `20914b6`
- `GOTCHA` L145 (2026-08-16) — `last_ingest` was declared in the state dict and never written.** The same dead-field bug as `SCHEMA_VERSION`, · `c31b311`
- `DECISION` L149 (2026-08-16) — Track the ingest lifecycle; make a re-run ask instead of assume.** A fan-out that dies at chapter 4 of 7 leave
- `REVERSAL` L159 (2026-08-16) — `--force` merges; it no longer resets.** It had overwritten the config wholesale, so a `local`-mode trace — lo
- `DECISION` L167 (2026-08-16) — MIT license.** WHY: a public repo with no license is all-rights-reserved, so nobody could legally use, fork or · `a52dd6f`
- `DECISION` L171 (2026-08-16) — Test the CLI as a subprocess, and sandbox `HOME`.** The exit code *is* the contract — `PreCompact` blocking is
- `LESSON` L179 (2026-08-16) — The suite found a real bug on its first run.** `status` counted a commit made in the *same second* as the trac · `b4fd479`
- `DECISION` L185 (2026-08-16) — CI on Linux and macOS both.** WHY: the two things most likely to differ across platforms are exactly what roeh · `47fe0d8`
- `DECISION` L190 (2026-08-16) — Prompt contracts are a separate, opt-in tier.** Four cases assert *mechanical* properties of the Oracle agains · `b70483e`
- `GOTCHA` L196 (2026-08-16) — `find_project_root` resolved to the wrong project entirely.** It exhausted each marker upward before trying th
- `LESSON` L206 (2026-08-16) — Dogfooding found what both the tests and the manual checks could not.** The root-resolution bug required a *re · `ddad17b`
- `OPEN` L211 (2026-08-16) — roeh's own conversational history is invisible to its own session mining.** Transcripts are filed by the *work
- `DECISION` L260 (2026-08-16) — Comment-sourced rationale is read at its commit, never at HEAD, and cited `file:line@sha`.** WHY: the honest o
- `LESSON` L271 (2026-08-16) — A comment that has drifted from its code is usually an unrecorded decision.** The property that makes comments
- `CORRECTION` L280 (2026-08-16) — They are not, reliably; reviewers skim them
- `DECISION` L291 (2026-08-17) — The README's competitive claims are researched, not asserted.** The owner's framing was that "virtually all ot
- `CORRECTION` L305 (2026-08-17) — They were appended before the work was committed, so they cite skill-file sections rather than a SHA
- `CORRECTION` L311 (2026-08-17) — It records v0.3.4
- `GOTCHA` L316 (2026-08-17) — Test invocations create session transcripts indistinguishable from real work.** Driving the Oracle with `claud
- `LESSON` L323 (2026-08-17) — The first drift check found the record's citations sound and its prose stale.** All twelve SHA citations resol
- `OPEN` L329 (2026-08-17) — This trace predates the `file:line@sha` convention** introduced at `391d34c`, so it carries zero comment-sourc
- `GOTCHA` L351 (2026-08-17) — `SessionStart` was injecting the SUPERSEDED §5.** `section()` took the first regex match, but in an append-onl
- `DECISION` L367 (2026-08-09) — The decision trace was commissioned in these words, at 2026-08-09 11:51pm local**, while the six archaeology a
- `LESSON` L401 (2026-08-09) — The origin statement survived only because the owner kept it and pasted it back.** It was not in a commit, a d
- `CORRECTION` L408 (2026-08-09) — ` immediately above]** That entry claimed the origin sentence "survived only because the owner kept it and pas
- `PRINCIPLE` L425 (2026-08-09) — The recursion, stated exactly.** Earlier entries describe it as "confident re-derivation," which is the sympto
