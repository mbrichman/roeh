# ROEH PROFILE — {{PROJECT}}

*Generated by `/roeh:ingest`, refreshed by `/roeh:refresh`. The `oracle` and `scribe`
read this to learn what THIS project's record is shaped like. The agent definitions hold
the invariant method; everything project-specific lives here, so a new project needs no
edits to the agents.*

---

## What this project is

<!-- Two or three sentences. What it does, what it is built on, who it is for.
     Enough that an agent reading only this file can tell a real finding from noise. -->

## The owner

<!-- How the record refers to the person whose decisions it records — "the owner", a
     handle, whatever the trace itself uses. Do NOT record real names of third parties
     here: an agent definition and its profile are prompts, and a name in a prompt is a
     leak surface. -->

## Sovereignty posture

- **local_only:** {{LOCAL_ONLY}}
- <!-- If true: what kind of material makes this necessary, in general terms. Enough for
       an agent to take the constraint seriously; never enough to be the leak itself. -->

## Vocabulary

<!-- Project-specific terms an agent will hit in the trace and misread without a gloss.
     One line each. e.g. "the cascade — fact-grain supersession, not a UI cascade". -->

| Term | Means |
|---|---|

## Standing principles (digest)

<!-- One line per §1 principle, so the oracle can pattern-match fast before the full
     read. The authoritative statement stays in the trace. -->

## LIVE dead-ends and reversals

<!-- The things someone is most likely to re-walk. The oracle leads with these.
     Keep it to what is still live — a dead-end that has itself been reversed belongs
     in the trace, not here. -->

| Don't re-walk | Why | Cite |
|---|---|---|

## Gate wiring

- **gate.enabled:** {{GATE_ENABLED}}
- <!-- If enabled: what the oracle is a clearance ON (PRs? merges to a branch?), who the
       other clearance is, and what a CONTRADICTS verdict obliges the caller to do. -->

## Where the rationale hides in this repo

<!-- Project-specific. Inline docstrings? ADRs? PR bodies? Commit trailers? The ingest
     and refresh passes mine these first. -->
