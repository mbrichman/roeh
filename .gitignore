__pycache__/
*.pyc
.DS_Store

# Per-machine watermarks. Listed here both because this repo is itself a roeh
# project, and so a copy-paste into a consuming project's .gitignore is one step.
.claude/roeh-state.json
.claude/roeh-pending.json

# derived read-model artifacts — regenerated by `roeh map`, never committed
docs/decision-trace-map.md
docs/decision-trace-bloom.json
